class Bill {
    constructor() {
    }
    /**
     * Calculates the bill.
     * @param {number} units
     * @param {1 | 2 | 3} type 1 = Normal, 2 = Commercial, 3 = Religious
     * @param {number} previous
     * @returns The bill value
     */
    calculate(units, type, previous) {
       return null;
    }
}

